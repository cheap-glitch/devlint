{
  "name": "devlint",
  "description": "A meta-linter to verify configuration files, projects structure and git repositories.",
  "version": "0.2.0",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cheap-glitch/devlint.git"
  },
  "bugs": {
    "url": "https://github.com/cheap-glitch/devlint/issues"
  },
  "author": "cheap glitch <cheap.glitch@gmail.com> (https://github.com/cheap-glitch)",
  "homepage": "https://github.com/cheap-glitch/devlint#readme",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "bin": {
    "devlint": "build/bin/devlint.js"
  },
  "files": [
    "src/**/*.ts",
    "build/src/**/*.js",
    "configs/*"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "tsc",
    "build:clean": "rimraf build",
    "build:prod": "npm run build",
    "build:watch": "tsc --watch",
    "coverage": "jest --collectCoverage",
    "coverage:upload": "coveralls < .coverage/lcov.info",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "lint": "eslint . --ext .ts --ignore-path .gitignore",
    "prepublishOnly": "npm run test && npm run build:clean && npm run build:prod",
    "test": "jest",
    "test:install": "npm run build:clean && npm run build && sudo npm uninstall -g devlint && sudo npm link",
    "test:watch": "jest --watch"
  },
  "eslintConfig": {
    "root": true,
    "extends": "@cheap-glitch/typescript",
    "plugins": [
      "tsdoc"
    ],
    "env": {
      "es6": true,
      "node": true
    },
    "rules": {},
    "overrides": [
      {
        "files": [
          "test/helpers.test.ts"
        ],
        "rules": {
          "space-in-parens": "off"
        }
      }
    ]
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "coverageDirectory": ".coverage",
    "collectCoverageFrom": [
      "src/lib/**/*.ts",
      "!src/lib/rules/*.ts",
      "build/src/lib/rules/*.js"
    ]
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "jsonast": "^0.2.1",
    "mazeru": "^1.0.2",
    "v8-compile-cache": "^2.2.0",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "@cheap-glitch/eslint-config-typescript": "^1.3.0",
    "@microsoft/tsdoc": "^0.13.0",
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.22",
    "@types/yargs": "^15.0.12",
    "eslint": "^7.18.0",
    "eslint-plugin-tsdoc": "^0.2.11",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.1.1",
    "type-fest": "^0.20.2",
    "typescript": "^4.1.3",
    "vitepress": "^0.11.4"
  }
}
